import React from 'react';
import {
    SafeAreaView,
    ScrollView,
    StatusBar,
    StyleSheet,
    Text,
    useColorScheme,
    View,
  } from 'react-native';

const CartIndex = () => {
    return(
        <View>
            <Text>CartIndex</Text>
        </View>
    );
}

export default CartIndex;